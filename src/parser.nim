import ./kin1, tables, strutils, sequtils

proc parse(program: string): Kobj =
    return kfn(kplus, ki(1), ki(4))

if isMainModule:
    echo "Parsing Module for K Language implementation in Nim [v0.0.1]"